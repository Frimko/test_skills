import styled from 'styled-components';

export const ContainerBody = styled.header`
    display: flex;
    position: relative;
    overflow: hidden;
`;

export const ContainerPage = styled.div`
  width: 100%;
`;
